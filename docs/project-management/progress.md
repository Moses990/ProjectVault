# 本地项目驾驶舱进度

## 2026-06-24

- 已确认用户需求：个人使用、统一管理项目资料、采用左侧列表 + 右侧详情、第一版先稳定、允许本地打开文件夹。
- 已读取 docs、README、SCHEMA、CHANGELOG 和样例项目数据。
- 已创建任务计划、调研记录和进度记录。
- 已新增 `code/web_dashboard.py`，提供零额外依赖的本地驾驶舱接口。
- 已新增 `web/index.html`、`web/styles.css`、`web/app.js`，实现左侧项目列表 + 右侧详情界面。
- 已新增 `scripts/启动项目驾驶舱.bat`。
- 已将本地网页驾驶舱入口和能力写入 `docs/README.md`。
- 已完成验证：首页、项目接口、项目详情接口、会议搜索接口、固定操作 `index` 均正常。
- 已完成界面重设计：三栏工作台、项目标签页、会议时间线、风险待办和搜索页。
- 开始 AI 只读助手 V0.1：只接外部 API，不自动修改 `project.json`。
- 已完成 AI 只读助手 V0.1：配置读取、OpenAI 兼容调用、状态接口、当前项目问答接口和前端 AI 标签页。
- 已验证无 API Key 时页面正常提示且不报前端错误。
- 已从电脑环境变量读取到 Agnes API Key，并写入私有配置 `config/local_settings.json`。
- 已验证 Agnes `agnes-2.0-flash` 可用，工作台 `/api/ai/status` 和项目助手问答接口均正常。
- 开始 AI 回答沉淀功能：回答先预览，用户点击保存后写入 `00_项目档案\ai_assistant\`，不修改 `project.json`。
- 已完成 AI 回答沉淀功能：后端保存接口、前端保存按钮、README 说明和保存流程验证均完成；测试文件已删除。
- 已完成 UI 重设计二次迭代：按工具型驾驶舱重新整理视觉系统，统一三栏布局、项目索引、摘要指标、证据列表、AI 助理控制区和文档式回答区；已通过桌面与窄屏浏览器截图检查。
- 已完成项目体检中心：后端新增待办进度和建议处理项，前端新增“体检”页签和“需处理”筛选；当前 `追觅` 项目待办进度为 0%，状态为高风险，主要原因是 5 条风险和 31 条未闭环会议待办。
- 已完成待办闭环中心：新增“待办闭环”页签，支持查看 31 条会议待办、按状态筛选，并将待办状态保存到 `00_项目档案\todo_status.json`；已验证状态保存和恢复流程。
